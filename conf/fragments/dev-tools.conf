BB_CONF_FRAGMENT_SUMMARY = "Narzedzia developerskie i serwer SSH w obrazie."
BB_CONF_FRAGMENT_DESCRIPTION = "Dokłada do obrazu lekki serwer SSH (dropbear) oraz narzedzia \
debugowe (gdb, strace) przez image feature tools-debug. Przeznaczone do buildow rozwojowych \
i CI - nie wlaczac w obrazach, ktore trafiaja do urzadzen produkcyjnych. \
"

IMAGE_FEATURES:append = " ssh-server-dropbear"
EXTRA_IMAGE_FEATURES:append = " tools-debug"
